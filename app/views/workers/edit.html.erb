<h1>Editing worker</h1>

<%= simple_form_for @worker, as: :worker, url: worker_path(@worker), html: { class: 'form' } do |f| %>
  <%= render 'form', f: f %>
<% end -%>

<br/>
<%= link_to 'Show', worker_path(@worker), class: 'btn btn-sm btn-default' %>
<%= link_to 'Back', workers_path, class: 'btn btn-sm btn-default' %>
