<%= simple_form_for(@site, html: { class: 'form' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :company_id, collection: Company.all %>
  <%= f.input :manager_id, collection: Manager.all.map {|manager| [manager.first_name,manager.id]} %>
  <%= f.button :submit %>
<% end %>
